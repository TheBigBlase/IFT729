#include "utils.hpp"
#include <iostream>

//Server::Server(uint16_t p)
//    : socket{context}, ws{socket}, port{p}
//{
//	context.run();
//	for (;;) {
//		acceptor.accept(socket);
//		do_receive();
//	}
//}
//
//Server::~Server()
//{
//}
//
//void Server::do_receive() {
//	for (;;)
//		ws.accept();
//	//std::size_t len = ws.read(data);
//	//std::cout << data.data().size() << std::endl;
//}

//Server::Server(uint16_t p) : socket(context, zmq::socket_type::pub) {
//
//};
